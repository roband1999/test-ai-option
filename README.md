# test-ai-work

A basic static webpage that includes a calculator UI, keyboard support, and a small Node test suite.

## Scripts

- `npm run build` checks that the required static assets exist.
- `npm test` runs the calculator logic tests with Node's built-in test runner.
- `npm start` serves the app locally at `http://localhost:4173`.
